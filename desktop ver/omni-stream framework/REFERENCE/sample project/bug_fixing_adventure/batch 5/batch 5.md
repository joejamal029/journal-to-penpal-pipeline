The core execution mechanics have been dispatched to the implementation bot, but our triage of the **Priority 3: Services & Integrations** layer is not over. 

We are moving into the task extension mechanics, state regressions, and data mapping edge cases. If delegation targets are blindly hardcoded or the state machine refuses to mathematically regress when a subtask is removed, the user loses critical control over their execution layer. Furthermore, we have severe data pollution occurring between the RSR and Pink Slip tracking systems that must be severed.

To protect the context window, here is the next strictly capped sub-batch for immediate execution. I am enforcing strict verbatim fidelity, exactly as documented in the master backlog.

***

### Batch 5: Priority 3: Services & Integrations - Task Extensions & Edge Cases (Part 2)

VIOLATION 2 — MISSING IMPLEMENTATION Spec Source: wlist_ui_spec.md, Interaction Patterns -> Delegation (→), "User can assign which list receives the delegation" File: src/services/taskService.ts Spec Trace: When delegating a task, the system must prompt or allow the user to designate which future list will receive the newly delegated assignment. Code Trace: The delegateTask function executes, hardcodes an offset of +1 day (tomorrow.getDate() + 1), generates a synthetic list ID for that exact date, and blindly pushes the task there. Divergence: The code removes all user agency in target list selection, strictly forcing all delegations to tomorrow's list. Evidence:

VIOLATION 4 — WRONG IMPLEMENTATION Spec Source: wlist_integrations.md, 3. Reinforced Spaced Repetition (RSR), "The SM-2 algorithm, adapted for W-List task integration." File: src/services/taskService.ts Spec Trace: Pink Slip tracking requires accurate read_type recording. Completing an RSR-driven task must correctly log it strictly under the rsr read type to maintain analytical integrity. Code Trace: When the UI grades an RSR completion, it triggers both processRsrReview (which correctly logs rsr) and updateTaskState. However, updateTaskState unconditionally evaluates if the task definition has a pink_slip_ref and forces a hardcoded revision read log. Divergence: The engine erroneously injects a duplicate, conflicting revision read log alongside every accurate rsr read for a completed RSR task. Evidence:

VIOLATION 4 — WRONG IMPLEMENTATION Spec Source: wlist_ui_spec.md, Screen 6: 3-Month List View (Task Library), "Sort by: last completed, frequency, points, name" File: src/services/taskService.ts Spec Trace: The Task Library filtering system must offer distinct, isolated sorting modalities to rank tasks explicitly by "last completed" (recency) versus "frequency" (total chronological completions). Code Trace: Inside the getLibraryTaskDefinitions database query builder, the logic evaluates the sortBy payload passed from the UI. When it encounters the 'frequency' flag, it immediately overrides the query logic to execute ORDER BY last_completed_at DESC. Divergence: The backend engine incorrectly hijacks the frequency sorting request to execute a last-completed recency sort, completely destroying the ability to sort by true frequency counts. Evidence:

VIOLATION 3 — MISSING IMPLEMENTATION Spec Source: wlist_ui_spec.md, Screen 8: Pomodoro Counter (task-embedded), "Useful for backdating: 'I did 2 pomodoros yesterday' → log 2, backdate" File: src/services/taskService.ts Spec Trace: The system explicitly highlights the ability to backdate the exact number of pomodoro sessions completed historically on a task to maintain accurate log allocations. Code Trace: The backdateTaskCompletion function signature receives the task ID, historical date, and state. It inserts the record while hardcoding pomodoro_points_earned based strictly on the completion state, entirely omitting a parameter or logic to capture and insert the pomodoros_logged integer. Divergence: The backdating service structurally lacks the capability to log historical pomodoro session counts, abandoning the feature explicitly cited in the spec. Evidence:

VIOLATION 3 — PARTIAL IMPLEMENTATION Spec Source: wlist_overview.md, Task State Machine, "case TaskState.COMPLETE: return to === TaskState.PARTIAL; // Allow regression if pomodoro is removed" File: src/services/taskService.ts Spec Trace: The state machine requires dynamic regression. If a parent task has been marked COMPLETE via reaching 100% completion, but a user action (like unchecking a subtask) drops the completion percentage below 100%, the parent task must mathematically regress back to a PARTIAL state. Code Trace: Inside toggleSubtaskState, the engine recalculates percent and evaluates the parent state. If percent < 1.0, it attempts to find the nextState using the conditional block else if (percent > 0 && parent.state === TaskState.PENDING). If the parent is already COMPLETE, this condition evaluates to false, leaving nextState stuck at COMPLETE. Divergence: The code fails to regress a parent task from COMPLETE to PARTIAL when a subtask is unchecked because the evaluation block blindly requires the prior state to be exactly PENDING. Evidence:

***

Dispatch these structural failures to the implementation bot immediately. Once the task service logic is fully reconciled, we have one final Service-layer batch covering Analytics queries and Goal restrictions before we finally breach the UI DOM trees.

Are you ready for the 'Next Batch'?