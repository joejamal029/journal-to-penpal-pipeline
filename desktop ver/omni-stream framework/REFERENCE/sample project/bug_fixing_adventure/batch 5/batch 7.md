The service controllers and pure engines are successfully conforming to the specification. We have officially breached the final architectural layer: **Priority 4: UI & Components**. 

A flawless backend is meaningless if the React Native JSX DOM drops user inputs, hides critical buttons, or fails to render the required visual semantics of the W-List philosophy. Because the UI domain is massive, I am strictly slicing it into logical sub-batches to prevent context overload.

We begin with the most critical interaction surface in the entire application: **Core Task Execution & Rendering** (The Today Screen and Task Rows). 

Here is the highest-priority UI batch for immediate execution. I am enforcing strict verbatim fidelity, exactly as written in the source document.

***

### Batch 7: Priority 4: UI & Components - Core Task Execution & Rendering

VIOLATION 1 — PARTIAL IMPLEMENTATION Spec Source: wlist_ui_spec.md, Screen 3: Audit View, "Next-day completion: strikethrough underline, BLUE color." File: src/components/TaskRow.tsx Spec Trace: When a task is deferred and then completed on the next day, the UI should render its text using blue ink with both a strikethrough and an underline applied. Code Trace: The task state router evaluates TaskState.DEFERRED_COMPLETE_NEXT_DAY, assigns the blue color correctly, but overrides the text decoration property to only apply line-through. Divergence: The implementation drops the underline requirement, only applying the strikethrough decoration. Evidence:

VIOLATION 1 — INVERTED RULE Spec Source: wlist_overview.md, Failure & Backlog Philosophy, "Manual corrections: States can be manually overridden (e.g., marking a missed task as complete with a reason)." File: src/components/TaskRow.tsx Spec Trace: The specification establishes a forgiving backlog philosophy that explicitly permits manual overrides to the state machine, allowing users to physically tap and manually correct a "missed" task to a "complete" status. Code Trace: The TaskRow component computes a strict canComplete boolean that acts as a rendering guard for the UI action buttons. If a task evaluates to TaskState.MISSED, this boolean unconditionally flips to false. Divergence: The UI actively hides the core completion ('W') button for missed tasks, structurally forbidding the user from executing the manual correction workflow explicitly demanded by the spec. Evidence:

VIOLATION 3 — MISSING IMPLEMENTATION Spec Source: wlist_ui_spec.md, Screen 1: Today View, The W Symbol, "Animated SVG path that progresses through these states. Tap cycles forward. Long-press accesses the full state picker." File: src/components/TaskRow.tsx Spec Trace: The electronic W Symbol must be a highly interactive control point on the task row. The user must be able to tap directly on the SVG itself to cycle its completion state or long-press it to open a comprehensive state picker. Code Trace: The TaskRow engine correctly imports and instantiates the <WSymbol /> component within renderIndicator. However, it limits the supplied properties exclusively to percentage, size, and color. Divergence: The implementation drops all interactive onPress and onLongPress callback hooks from the <WSymbol /> declaration, completely severing the required tap-to-cycle input physics. Evidence:

VIOLATION 2 — MISSING IMPLEMENTATION Spec Source: 4. daily.md, Underlining: To show deference, "-/-/- -> Partial underlining and crossing out for incomplete tasks used to differentiate tasks not done at all." File: src/components/TaskRow.tsx Spec Trace: The physical visual language dictates that partially completed tasks must receive a distinct partial underline and crossing out to visually separate them from completely ignored tasks. Code Trace: The styling router evaluates the TaskState string. While it explicitly assigns text decoration lines for deferred and completed states, TaskState.PARTIAL falls completely through to the default block, leaving textDecorationLine unassigned (defaulting to 'none'). Divergence: The UI engine ignores styling partial tasks, failing to apply the mandatory partial underline and crossing out. Evidence:

VIOLATION 4 — MISSING IMPLEMENTATION Spec Source: 4. daily.md, "The total number of points for all tasks on the list is written at the bottom of the list" File: src/screens/TodayScreen.tsx Spec Trace: The visual representation of a daily list strictly requires the aggregated sum of all points for all assigned tasks to be calculated and displayed at the absolute bottom of the list view. Code Trace: The renderPane function constructs the daily list container by wrapping a ScrollView around a .map() iteration of SectionGroup components. When the iteration ends, the ScrollView closes without calculating or appending any <Text> node to display the aggregate list points. Divergence: The UI completely omits the mathematical summation footer required at the bottom of the daily list constraint block. Evidence:

***

Dispatch these missing interaction hooks and styling modifiers to the implementation bot immediately. The user must be able to physically cycle states and read the visual semantics before we move on to macro list building.

Are you ready for the 'Next Batch'?